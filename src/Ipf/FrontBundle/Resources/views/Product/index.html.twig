{% extends 'IpfFrontBundle:layout:layout.html.twig' %}

{% block sideCategory %}
    <div class="col-md-3">
        <p class="lead">Ulysse</p>
        <div class="list-group">
            <a href="#" class="list-group-item">Category 1</a>
            <a href="#" class="list-group-item">Category 2</a>
            <a href="#" class="list-group-item">Category 3</a>
        </div>
    </div>
    <br/>
    <br/>
    <br/>
{% endblock %}
{% block body -%}
    <div class="col-md-9">
        <div class="row">
            {% for entity in entities %}
                {{ dump(entity) }}
            <div class="col-sm-4 col-lg-4 col-md-4">
                <div class="thumbnail">
                    <img src="http://placehold.it/320x150" alt="">
                    <div class="caption">
                       {# <h4><a href="#">{{ entity.productName }}</a>
                        </h4>
                        <p>{{ entity.productDescription }}</p>
                        <p>{{ entity.productValidated }}</p>
                        <a href="{{ path('product_show', { 'id': entity.productId }) }}">show</a>#}
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>
    </div>
    {% endblock %}


