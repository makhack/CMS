{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Producttag list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Producttagid</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('producttag_show', { 'id': entity.producttagId }) }}">{{ entity.producttagId }}</a></td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('producttag_show', { 'id': entity.producttagId }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('producttag_edit', { 'id': entity.producttagId }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('producttag_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
